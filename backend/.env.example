# ─── backend/.env.example ─────────────────────────────────────
# Copy to backend/.env for local development

# ── Database (choose one mode) ─────────────────────────────────

# Mode A: PostgreSQL via local Cloud SQL Auth Proxy
DB_DRIVER=postgresql+asyncpg
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=meddent
DB_USER=meddent_app
DB_PASSWORD=devpassword

# Mode B: Cloud SQL via Unix socket (Cloud Run / Docker with proxy socket)
# DB_DRIVER=postgresql+asyncpg
# CLOUD_SQL_CONNECTION_NAME=my-project:us-central1:meddent-db
# USE_UNIX_SOCKET=true
# DB_NAME=meddent
# DB_USER=meddent_app
# DB_PASSWORD=...

# Mode C: SQLite (no Postgres needed — great for quick local test)
# DB_DRIVER=sqlite+aiosqlite

# ── AI ─────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── CORS ───────────────────────────────────────────────────────
ALLOWED_ORIGINS=http://localhost:3000
